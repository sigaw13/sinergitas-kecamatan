<!DOCTYPE html>
<html lang="id">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Top 5 Rekap Nilai Wawancara SIESELON</title>
  <style>
    * { box-sizing:border-box; margin:0; padding:0; }
    body { font-family:"Segoe UI", Tahoma, Geneva, Verdana, sans-serif; background:#f4f6fb; color:#273142; }
    .navbar { background:linear-gradient(135deg,#5d73df,#7047a5); color:#fff; padding:14px 26px; display:flex; justify-content:space-between; align-items:center; gap:12px; flex-wrap:wrap; box-shadow:0 2px 12px rgba(0,0,0,.12); }
    .navbar h1 { font-size:20px; }
    .nav-actions { display:flex; gap:8px; align-items:center; flex-wrap:wrap; }
    .navbar a { color:#fff; text-decoration:none; background:rgba(255,255,255,.17); padding:8px 12px; border-radius:7px; font-size:13px; }
    .container { max-width:1500px; margin:28px auto; padding:0 18px; }
    .card, .table-card { background:#fff; border-radius:13px; box-shadow:0 3px 14px rgba(25,42,70,.08); padding:22px; margin-bottom:22px; }
    .intro h2 { margin-bottom:8px; }
    .muted { color:#687386; line-height:1.55; }
    .message { padding:13px 15px; border-radius:8px; margin-bottom:16px; }
    .success { background:#d7f2df; color:#166534; border-left:4px solid #22a447; }
    .error { background:#f9d9df; color:#922b3e; border-left:4px solid #dc3545; }
    .grid { display:grid; grid-template-columns:repeat(auto-fit,minmax(220px,1fr)); gap:16px; margin-top:16px; }
    .metric-card { background:#f7f8fc; border-radius:10px; padding:16px; border-left:5px solid #5d73df; }
    .metric-card h3 { font-size:14px; color:#4d586b; margin-bottom:8px; }
    .metric { font-size:32px; font-weight:800; color:#5e70d9; }
    .btn { display:inline-block; border:0; border-radius:7px; padding:9px 14px; text-decoration:none; color:#fff; background:linear-gradient(135deg,#5d73df,#7047a5); font-size:13px; cursor:pointer; }
    .btn-secondary { background:#687386; }
    .table-card { overflow-x:auto; }
    table { width:100%; border-collapse:collapse; min-width:1150px; border:2px solid #111; }
    th,td { padding:9px 8px; border:1px solid #111; vertical-align:middle; font-size:12px; }
    th { background:#fff; color:#000; text-align:center; font-weight:800; line-height:1.35; }
    td { background:#fff; color:#000; }
    td.center { text-align:center; }
    td.right { text-align:right; }
    td.name { font-weight:500; min-width:190px; text-transform:uppercase; }
    .rank { font-weight:800; text-align:center; }
    .actions { display:flex; gap:10px; flex-wrap:wrap; align-items:center; margin-top:16px; }
    .note { padding:12px; background:#fffaf0; border-left:4px solid #efb400; border-radius:8px; margin-top:12px; color:#6b5200; font-size:12px; line-height:1.5; }
    @media (max-width:720px) { .navbar { padding:12px 15px; } .container { padding:0 10px; } }
  </style>
</head>
<body>
  <nav class="navbar">
    <h1>🎙️ Top 5 Rekap Nilai Wawancara</h1>
    <div class="nav-actions">
      <span>👤 <%= username %> — Superadmin</span>
      <a href="/dashboard">📊 Dashboard</a>
      <a href="/evaluation">✅ Verifikasi</a>
      <a href="/ranking">🏆 Peringkat</a>
      <a href="/official-report">📑 Hasil Resmi</a>
      <a href="/logout">Keluar</a>
    </div>
  </nav>

  <main class="container">
    <% if (success) { %><div class="message success">✅ Rekap nilai wawancara berhasil disimpan.</div><% } %>
    <% if (error) { %><div class="message error">⚠️ <%= error %></div><% } %>

    <section class="card intro">
      <h2>Top 5 Rekap Nilai Akhir Hasil Wawancara</h2>
      <p class="muted">Halaman ini hanya menampilkan 5 kecamatan teratas sesuai format rekap wawancara.</p>
      <div class="note">
        Rumus: Capaian Wawancara = Total Wawancara / 400 × 100. Nilai Akhir = 50% Capaian Wawancara + 50% Capaian Input Data/Survey Lapangan.
      </div>
      <div class="grid">
        <div class="metric-card"><h3>Total Kecamatan Dinilai</h3><div class="metric"><%= allRows.filter(row => row.final && row.final.finalScore > 0).length %></div></div>
        <div class="metric-card"><h3>Ditampilkan</h3><div class="metric"><%= rows.length %></div></div>
        <div class="metric-card"><h3>Nilai Tertinggi</h3><div class="metric"><%= rows.length ? rows[0].final.finalScore.toFixed(2) : '0.00' %></div></div>
      </div>
    </section>

    <section class="table-card">
      <h2>5 Teratas Nilai Akhir Wawancara</h2>
      <p class="muted" style="margin:8px 0 14px">Data diurutkan berdasarkan peringkat akhir tertinggi.</p>
      <table>
        <thead>
          <tr>
            <th>NO</th>
            <th>KECAMATAN</th>
            <th>ASPEK<br>PENAMPILAN DAN<br>PENGUASAAN MATERI</th>
            <th>ASPEK PENGAYAAN<br>AKSI NYATA/KOLABORASI<br>NON APBD YANG LANGSUNG<br>DIRASAKAN OLEH MASYARAKAT</th>
            <th>TOTAL</th>
            <th>CAPAIAN<br>WAWANCARA<br>(%)</th>
            <th>CAPAIAN<br>WAWANCARA<br>(BOBOT NILAI 50%)</th>
            <th>CAPAIAN INPUT<br>DATA DAN SURVEY<br>LAPANGAN<br>(BOBOT NILAI 50%)</th>
            <th>PERINGKAT</th>
            <th>Rank</th>
          </tr>
        </thead>
        <tbody>
          <% if (!rows.length) { %>
            <tr>
              <td colspan="10" class="center">Belum ada data rekap wawancara. Jalankan import Excel atau simpan nilai wawancara terlebih dahulu.</td>
            </tr>
          <% } %>
          <% rows.forEach((row, index) => { const final = row.final || {}; %>
            <tr>
              <td class="center"><%= index + 1 %></td>
              <td class="name"><%= row.nama %></td>
              <td class="right"><%= Number(final.presentationTotal || 0).toFixed(2) %></td>
              <td class="right"><%= Number(final.collaborationTotal || 0).toFixed(2) %></td>
              <td class="right"><%= Number(final.interviewTotal || 0).toFixed(2) %></td>
              <td class="right"><%= Number(final.interviewPercentage || 0).toFixed(0) %></td>
              <td class="right"><%= Number(final.interviewWeightedScore || final.interviewPercentage || 0).toFixed(0) %></td>
              <td class="right"><%= Number(final.inputDataScore || 0).toFixed(2) %></td>
              <td class="right"><%= Number(final.finalScore || 0).toFixed(2) %></td>
              <td class="rank"><%= final.finalRank || index + 1 %></td>
            </tr>
          <% }) %>
        </tbody>
      </table>
      <div class="actions">
        <a href="/interview-recap/export.csv" class="btn">📥 Ekspor CSV Top 5</a>
        <a href="/dashboard" class="btn btn-secondary">Kembali ke Dashboard</a>
      </div>
    </section>
  </main>
</body>
</html>
